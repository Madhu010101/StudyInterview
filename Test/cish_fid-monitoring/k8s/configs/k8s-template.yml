---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-all-egress
  namespace: {{ k8s_project }}
spec:
  podSelector: {}
  egress:
  - {}
  policyTypes:
  - Egress
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ app_name }}
  labels:
    app: {{ app_name }}
spec:
  replicas: {{ replicas }}
  selector:
    matchLabels:
      app: {{ app_name }}
  template:
    metadata:
      labels:
        app: {{ app_name }}
    spec:
      imagePullSecrets:
{% if k8s_env != "prod" %}
      - name: npharbor
{% endif %}
{% if k8s_env == "prod" %}
      - name: harbor
{% endif %}
      containers:
      - name: {{ app_name }}
        image: docker-registry.default.svc:5000/{{ k8s_project }}/{{ app_name }}:{{ tag }}
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: {{ internal_port }}
        resources:
            limits:
              memory: 512Mi
              cpu: 450m
            requests:
              memory: 200Mi
              cpu: 100m
        envFrom:
        - configMapRef:
            name: {{ app_name }}-configmap
        - secretRef:
            name: {{ app_name }}-secrets

---
apiVersion: v1
kind: Service
metadata:
  name: {{ app_name }}
  labels:
    app: {{ app_name }}
spec:
   type: NodePort
   ports:
     - port: {{ internal_port }}
       targetPort: {{ internal_port }}
   selector:
     app: {{ app_name }}

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ app_name }}-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/proxy-body-size: 100m
spec:
  tls:
  - hosts:
      - {{ app_name }}-{{ k8s_project }}.cit-{{ rancher_env }}.fnb.co.za
    secretName: tlssecret
  rules:
  - host: "{{ app_name }}-{{ k8s_project }}.cit-{{ rancher_env }}.fnb.co.za"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: {{ app_name }}
            port:
              number: {{ internal_port }}


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: {{ app_name }}-ingress
#   annotations:
#     kubernetes.io/ingress.class: "nginx"
#     nginx.ingress.kubernetes.io/proxy-body-size: 100m
# spec:
#   tls:
#   - hosts:
# {% if rancher_env == "dev" %}
#       - cit-k8s-{{ rancher_env }}.fnb.co.za
# {% else %}
#       - {{ app_name }}-{{ k8s_project }}.cit-{{ rancher_env }}.fnb.co.za
# {% endif %}
#     secretName: tlssecret
#   rules:
# {% if rancher_env == "dev" %}
#   - host: "cit-k8s-{{ rancher_env }}.fnb.co.za"
# {% else %}
#   - host: "{{ app_name }}-{{ k8s_project }}.cit-{{ rancher_env }}.fnb.co.za"
# {% endif %}
#     http:
#       paths:
# {% if rancher_env == "dev" %}
#       - path: /{{ app_name }}-{{ k8s_project }}
# {% else %}
#       - path: /
# {% endif %}
#         pathType: Prefix
#         backend:
#           service:
#             name: {{ app_name }}
#             port:
#               number: {{ internal_port }}