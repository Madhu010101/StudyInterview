apiVersion: v1
kind: ConfigMap
metadata:
  name: template
data:
  transform.ftl: >-
    <#assign aDateTime = .now>
    <#assign aDate = aDateTime?date>
    <#assign aTime = aDateTime?time>

    ${body}